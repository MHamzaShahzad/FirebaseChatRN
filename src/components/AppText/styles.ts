import {StyleSheet} from 'react-native';
import colors from '../../theme/colors';
import {Font} from '../../helpers/scalling';

export const styles = StyleSheet.create({
  text: {
    fontSize: Font(15),
    color: colors.black,
  },
});
